# Football-Stat-Visualizer
This program recieves data detailing the goal counts of Football (soccer) players within a team during a season and visualizes/sorts it. The data can either be retrieved from API-Football (with a valid API key) or from the provided JSON files.
